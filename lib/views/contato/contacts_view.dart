import 'package:flutter/widgets.dart';
//pagina dos contatos
class ContactsView extends StatelessWidget {
  const ContactsView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Chama no zap'),); 
  }
} 